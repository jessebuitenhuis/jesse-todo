export enum BorderRadius {
  Default = "0.7em",
}

export enum Padding {
  Default = "1em",
}

export const Grey = {
  "100": "lightgrey",
};

export const Colors = {
  Border: Grey[100],
};

export const Border = {
  Default: `1px solid ${Colors.Border}`,
};
